
// Key linked to the Metadata Properties
export const KEY_PROPS     = Symbol('key_props');

export const decorators = {
    // Key of @Type()
    KEY_TYPE        : Symbol('key_type'),

    // Key of @Required()
    KEY_REQUIRED    : Symbol('key_required'),

    // Key of @Strip()
    KEY_STRIP       : Symbol('key_strip'),

    // Key of @Valid()
    KEY_VALID       : Symbol('key_valid'),

    // Key of @Invalid()
    KEY_INVALID     : Symbol('key_invalid'),

    // Key of @Invalid()
    KEY_ALLOW       : Symbol('key_allow'),

    // Key of @Description()
    KEY_DESCRIPTION : Symbol('key_decription')
}
